package fr.univtln.mgajovski482.d12.User.RegisteredUser.RegisteredUserFactory;

import fr.univtln.mgajovski482.d12.Other.Consts;

import java.io.*;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

/**
 * Created by Maxime on 18/10/2015.
 */
public class RegUserFactory {
/*
    public static void importNamesFromFile() {
        int i = 0;
        try {
            File file                       = new File(Consts.RESOURCES_PATH + "Names.txt");
            FileReader fileReader           = new FileReader(file);
            BufferedReader bufferedReader   = new BufferedReader(fileReader);

            String[] currentLine;
            currentLine  = bufferedReader.readLine().split(" ");

            while ((currentLine = bufferedReader.readLine()) != null) {

                for(String currentWord : currentLine)
                    System.out.println(currentWord);

                currentLine = bufferedReader.readLine().split(" ");
                System.out.println("\n");
            }

            bufferedReader.close();

        } catch (FileNotFoundException e1) {
            e1.printStackTrace();
        } catch (IOException e1) {
            e1.printStackTrace();
        } catch (Exception e) {
            System.out.println("i = " + i);
            throw new Error(e);
        }

    }
*/
}
